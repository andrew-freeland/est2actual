# ============================================
# ESTIMATE INSIGHT - ENVIRONMENT VARIABLES
# ============================================

# --------------------------------------------
# REQUIRED: Google Cloud Project Configuration
# --------------------------------------------

# Your Google Cloud Project ID
# Find this at: https://console.cloud.google.com/
# Example: my-project-12345
GCP_PROJECT_ID=your-project-id-here

# Google Cloud Region (where your services run)
# Recommended: us-central1 (lowest latency for Vertex AI)
GCP_REGION=us-central1

# --------------------------------------------
# REQUIRED: Google Cloud Authentication
# --------------------------------------------

# Path to your service account key JSON file
# Download from: https://console.cloud.google.com/iam-admin/serviceaccounts
# IMPORTANT: Keep this file secure and NEVER commit it to git!
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Alternatively, for local development you can use:
# gcloud auth application-default login
# Then comment out the line above or leave it empty

# --------------------------------------------
# OPTIONAL: Flask Configuration
# --------------------------------------------

# Flask secret key for sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Flask environment (development or production)
FLASK_ENV=development

# Port for local development
PORT=8080

# --------------------------------------------
# OPTIONAL: Service Configuration
# --------------------------------------------

# Service name for Cloud Run deployment
SERVICE_NAME=estimate-insight-agent

# --------------------------------------------
# NOTES & TROUBLESHOOTING
# --------------------------------------------

# Common Issues:
# 
# 1. "Permission denied" or 505 errors:
#    - Ensure your service account has these roles:
#      • Vertex AI User (roles/aiplatform.user)
#      • Cloud Datastore User (roles/datastore.user)
#      • Storage Object Viewer (roles/storage.objectViewer)
#
# 2. "Project not found":
#    - Double-check your GCP_PROJECT_ID matches exactly
#    - Verify you have access to the project
#
# 3. "API not enabled":
#    - Enable Vertex AI API: gcloud services enable aiplatform.googleapis.com
#    - Enable Firestore: gcloud services enable firestore.googleapis.com
#
# 4. Local development authentication:
#    - Run: gcloud auth application-default login
#    - Or use a service account key file
